"use client";
import { useEffect, useState } from "react"; import AppBar from "../components/AppBar"; import BottomNav from "../components/BottomNav"; import { useRequireSuperAdmin } from "../hooks/useRequireApp"; import { supabase } from "../../lib/supabaseClient";
type C={id:string;name:string;type:string;slug:string};
export default function Superadmin(){const app=useRequireSuperAdmin();const[rows,setRows]=useState<C[]>([]);useEffect(()=>{supabase.from("competitions").select("id,name,type,slug").order("name").then(({data})=>setRows((data??[]) as C[]));},[]);return <><AppBar league="FantaChat" team="SUPERADMIN" onMenuOpen={app.openDrawer}/><main style={s.container}><div style={s.card}><h1>Superadmin</h1><p>Dati globali validi per tutte le leghe: competizioni, stagioni, squadre reali, giocatori, partite, top N, voti.</p></div>{rows.map(c=><div key={c.id} style={s.card}><h2>{c.name}</h2><div style={s.grid}><a href={`/superadmin/competizioni/${c.id}/partite`}>Partite</a><a href={`/superadmin/competizioni/${c.id}/top-n`}>Top N</a><a href={`/superadmin/competizioni/${c.id}/voti`}>Voti</a></div></div>)}{!rows.length&&<div style={s.card}>Nessuna competizione globale. Esegui i seed Supabase.</div>}</main><BottomNav/></>}
const s:Record<string,React.CSSProperties>={container:{maxWidth:520,margin:"0 auto",padding:"16px 14px 100px",display:"grid",gap:12},card:{background:"white",border:"1px solid #e5e7eb",borderRadius:18,padding:16},grid:{display:"grid",gridTemplateColumns:"1fr 1fr 1fr",gap:8},};
